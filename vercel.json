{
  "version": 2,
  "projects": {
    "frontend": {
      "rootDirectory": "frontend",
      "buildCommand": "npm run build",
      "outputDirectory": "dist",
      "framework": "vite"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}